
define m = 0;

int v()
{
	void buff;
	fgets(&buff, 0x200, stdin);
	printf(&buff);
	if (m == 0x40)
	{
		printf("Wait what?!\n");
		return (system("/bin/sh"));
	}
	return 0;
}

int main()
{
	return(v);
}
